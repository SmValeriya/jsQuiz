.btn {
  padding: 10px 15px;
  border-radius: 5px;
  font-size: 1.1rem;
  font-weight: 600;
  color: $text-color-light;
  background-color: $primary;

  &:hover {
    filter: brightness(1.2) drop-shadow(0 2px 2px $primary);
  }
}
